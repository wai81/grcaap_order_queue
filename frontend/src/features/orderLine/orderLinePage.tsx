import { observer } from "mobx-react-lite"
import OrderLineSearch from "../../app/componets/orderLineSearch/orderLineSearch";

export default observer(function OrderLine() {
    return (
        <>
            <OrderLineSearch />
        </>
    )

});
